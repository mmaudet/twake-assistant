# Twake Assistant Configuration

# Backend API URL for Whisper transcription
# Default: http://localhost:8000 (local MLX backend)
# You can change this to point to a remote server
WHISPER_BACKEND_URL=http://localhost:8000
